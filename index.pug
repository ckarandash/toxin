include @components/form-elements/text-field/text-field.pug
include @components/form-elements/dropdown/dropdown.pug

doctype html
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title Document
    script(src='./index.js' defer)
    link(href=require('./main.sass') rel='stylesheet')
  body 
    //- +text-field({hasArrow: true})
    //- +text-field({placeholder: "Email"})
    //- +text-field({isMasked: true})

    +dropdown({
      question: 'Сколько гостей',
      items: [
        { label: 'спальни', initialCount: 0, name: 'bedrooms', word: 'спальня' },
        { label: 'кровати', initialCount: 0, name: 'beds', word: 'кровать' },
        { label: 'ванные комнаты', initialCount: 0, name: 'bathrooms', word: 'ванная комната' },
      ],
      withClear: true,
      withApply: true,
    })