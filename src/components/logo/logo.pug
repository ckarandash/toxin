mixin logo(options = {})
  - const { linkTo } = options;

  .logo
    if linkTo
      a.logo__link(href=linkTo)
        img.logo__image(src=require("@assets/images/logo.svg"), alt="логотип")
    else
      img.logo__image(src=require("@assets/images/logo.svg"), alt="логотип")